# FastAPI + Server
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# Database
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
aiosqlite==0.20.0
alembic==1.14.1
psycopg2-binary==2.9.10

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.1

# Pydantic
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0

# Celery + Redis
celery[redis]==5.4.0
redis==5.2.1

# Data Analysis
pandas==2.2.3
numpy==1.26.4
openpyxl==3.1.5

# Visualization
plotly==5.24.1
kaleido==0.2.1

# PDF Export
weasyprint==63.1

# AI / LLM
langchain==0.3.14
langchain-community==0.3.14
langchain-openai==0.3.1
langchain-groq==0.2.3
langchain-google-genai==2.0.9
langchain-ollama==0.2.0
langgraph==0.2.60
langgraph-checkpoint-redis>=0.1.0
fastembed>=0.4.0

qdrant-client>=1.11.0
pypdf==5.1.0
pdf2image==1.17.0

# Advanced RAG (Vision/Descriptions)
pillow>=10.4.0

# Encryption
cryptography==44.0.0

# Testing
pytest==8.3.4
pytest-asyncio==0.25.0
httpx==0.28.1

# Rate Limiting
slowapi==0.1.9

# Logging
structlog==24.4.0

# Fast Text RAG (kept as fallback)
PyMuPDF==1.25.1

# Neo4j Graph DB
neo4j==5.21.0

# ── Universal Document Intelligence ───────────────────────────────────────────
# Unstructured.io — intelligent element-aware document parsing
# Supports: PDF, DOCX, PPTX, XLSX, HTML, EPUB, EML, MSG, TXT, MD, RTF, ODT,
#           CSV, TSV, PNG, JPG (via OCR strategy)
unstructured[pdf,docx,pptx,xlsx,md,html,epub,eml,odt,rtf,csv,tsv]>=0.16.0

# Required by Unstructured for DOCX/PPTX parsing
python-docx>=1.1.2
python-pptx>=1.0.0

# MIME type detection — used by Unstructured auto-partitioner
python-magic-bin>=0.4.14; sys_platform == 'win32'
python-magic>=0.4.27; sys_platform != 'win32'

# ── RAG Evaluation ────────────────────────────────────────────────────────────
# Local Galileo-equivalent metrics (Chunk Relevance, Attribution, Utilization)
sentence-transformers>=3.0.0
torch>=2.2.0
